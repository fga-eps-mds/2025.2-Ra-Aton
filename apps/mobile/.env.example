# Template de Configuração Local
# Copie este arquivo para .env.local e configure com seu IP

# API URL - Use o IP da sua máquina quando testar no celular físico
# localhost funciona apenas no emulador/simulador
EXPO_PUBLIC_API_URL=http://localhost:4000

# Ambiente
EXPO_PUBLIC_ENV=development

# ============================================
# COMO DESCOBRIR SEU IP:
# ============================================
# 
# Linux/Mac:
#   ifconfig | grep "inet " | grep -v 127.0.0.1
#   ou
#   ip addr show | grep "inet " | grep -v 127.0.0.1
#
# Windows:
#   ipconfig | findstr IPv4
#
# Exemplo de IP: 192.168.1.100
# ============================================
#
# IMPORTANTE:
# - Seu celular e computador DEVEM estar na mesma rede WiFi
# - Use http:// não https://
# - Inclua a porta (geralmente 4000 para o backend)
#
# Exemplos:
# EXPO_PUBLIC_API_URL=http://192.168.1.100:4000    # WiFi casa
# EXPO_PUBLIC_API_URL=http://10.0.0.5:4000         # WiFi trabalho
# EXPO_PUBLIC_API_URL=http://localhost:4000        # Emulador
